class Agent:
    """A simple coding agent placeholder."""

    def __init__(self, name: str = "Hyperion"):
        self.name = name

    def greet(self) -> str:
        """Return a greeting from the agent."""
        return f"Hello, I am {self.name}!"
